import type { Meta, StoryObj } from '@storybook/react';
import Button2 from '../components/Button2';

const meta: Meta<typeof Button2> = {
  title: 'examples/Button2',
  component: Button2,
};

export default meta;

type Story = StoryObj<typeof Button2>;

export const Primary: Story = {}
